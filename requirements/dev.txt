# CPU version of PyTorch for Python 3.12

# Core dependencies - 核心依赖
# PyTorch框架，用于OCR模型支持
torch==2.4.1
# PyTorch计算机视觉扩展库（当前项目未直接使用）
torchvision==0.19.1

# Windows-specific dependencies - Windows特定依赖
# Windows API调用，用于窗口管理和截图
pywin32==311
# 图像处理库，用于中文文本绘制和图像操作
pillow==12.0.0
# 进程管理库，用于获取进程信息
psutil==7.1.3
# 剪贴板操作库，用于读写剪贴板内容
pyperclip==1.11.0
# Windows输入模拟库，用于键盘和鼠标操作
PyDirectInput==1.0.4

# OCR and vision dependencies - OCR和视觉依赖
# OCR文字识别库，用于图像文本提取
easyocr==1.7.2
# GUI框架，用于构建桌面应用界面
PyQt6==6.10.1
# OpenCV计算机视觉库，用于图像处理和计算机视觉
opencv-contrib-python==4.12.0.88
# 图像处理库，用于计算截图相似度
scikit-image==0.25.2

# Development tools - 开发工具
# 代码质量检查工具
pylint==4.0.4
# Pylint的底层分析库
astroid==4.0.2
# 代码导入排序工具
isort==7.0.0

# Hardware acceleration - 硬件加速
# 用于硬件加速截图的库
dxcam==0.0.5