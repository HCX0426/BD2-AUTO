# BD2-AUTO 生产环境配置（.env.prod）
# 说明：打包时嵌入，用户无需修改，仅用于生产环境默认参数

ENV=prod
WINDOWS_APP_TITLE=BD2-游戏窗口  # 默认游戏窗口标题（用户可通过GUI修改）
OCR_ENGINE=paddleocr
OCR_CONFIDENCE=0.65
LOG_LEVEL=INFO
LOG_MAX_SIZE=5
LOG_BACKUP_COUNT=3
TASK_TIMEOUT=600
RETRY_COUNT=2
CLICK_INTERVAL=0.3
DEBUG_MODE=False
SKIP_SLEEP=False